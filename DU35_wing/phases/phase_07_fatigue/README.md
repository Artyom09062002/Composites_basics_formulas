# Phase 7 — Fatigue

Transfers source-based 20-year damage, applies the documented DB and H100
curves, and closes the combined `b=9` plus `A66h ±10%` sensitivity envelope.
The accepted target is `D20 <= 0.70`.

The numerical curve provenance is documented in `data/db_h100_sources.md`.
